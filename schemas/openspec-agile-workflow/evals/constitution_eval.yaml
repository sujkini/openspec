stage: constitution
template: templates/constitution.md
artifact: constitution.md
version: 1
eval_count: 6
evals:
- id: eval-r001-const-001
  round: 1
  stage: constitution
  source_issue_ids: []
  patterns:
  - PAT-001
  input_refs:
  - evals/inputs/01-ep-ard.md
  prompt: 'Constitution must not claim cluster-scoped singleton if EP specifies namespaced default singleton.

    '
  assertions:
    must_mention:
    - controller-runtime
    - namespaced
    - default
    must_not_claim:
    - metadata.name == cluster
    must_cite_repo_evidence: true
  scoring:
    method: weighted_checklist
    pass_threshold: 80
- id: eval-r001-const-002
  round: 1
  stage: constitution
  source_issue_ids:
  - ISSUE-001
  patterns:
  - PAT-003
  input_refs:
  - evals/baseline/agents.md
  prompt: 'Constitution must mandate single unified ctrl.Manager for all addon controllers per repo conventions.

    '
  assertions:
    must_mention:
    - setup_manager.go
    - unified manager
    - must not create separate managers
    must_align_with_agents_md: true
  scoring:
    method: weighted_checklist
    pass_threshold: 85
- id: eval-r001-const-003
  round: 1
  stage: constitution
  source_issue_ids: []
  patterns:
  - PAT-002
  input_refs:
  - evals/inputs/01-ep-ard.md
  prompt: 'Constitution must document limited teardown on CR delete as governance principle from EP non-goals.

    '
  assertions:
    must_mention:
    - manual cleanup
    - stop reconciling
    - non-goal
    must_not_claim:
    - full cleanup on CR delete
  scoring:
    method: weighted_checklist
    pass_threshold: 80
- id: eval-r002-const-001
  round: 2
  stage: constitution
  source_issue_ids:
  - ISSUE-007
  patterns:
  - PAT-011
  input_refs:
  - evals/baseline/agents.md
  prompt: 'Constitution must require compare-before-update for managed resources to prevent hot reconcile loops.

    '
  assertions:
    must_mention:
    - semantic equality
    - unconditional update
    - reconcile loop
    must_cite_repo_evidence: true
  scoring:
    method: weighted_checklist
    pass_threshold: 85
- id: eval-r002-const-002
  round: 2
  stage: constitution
  source_issue_ids: []
  patterns:
  - PAT-009
  input_refs:
  - evals/inputs/01-ep-ard.md
  prompt: 'Constitution must document when library-go patterns apply vs controller-runtime (cert-manager core vs addons).

    '
  assertions:
    must_mention:
    - library-go
    - certmanager core
    - do not mix patterns
  scoring:
    method: weighted_checklist
    pass_threshold: 80
- id: eval-r002-const-003
  round: 2
  stage: constitution
  source_issue_ids: []
  patterns:
  - PAT-013
  input_refs:
  - evals/inputs/01-ep-ard.md
  prompt: 'Constitution must note opt-in security features default false for backward compatibility.

    '
  assertions:
    must_mention:
    - defaultNetworkPolicy
    - opt-in
    - backward compatibility
  scoring:
    method: weighted_checklist
    pass_threshold: 80
