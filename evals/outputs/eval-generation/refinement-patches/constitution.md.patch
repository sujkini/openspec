--- constitution.md (prior backfill)
+++ constitution.md (full bundle backfill)
@@ Addon controllers
+Singleton semantics: namespaced default vs cluster
+CR delete / teardown: limited — manual cleanup non-goal
+Status conditions: Ready when healthy, Degraded on failure
+OLM upgrade: N-1 → N testable when new CRD joins bundle
+Operand version matrix: bindata vs OSSM/Istio minimums
+Documentation placeholders: consistent naming across examples
@@ Network policy
+Opt-in security defaults: defaultNetworkPolicy false for backward compat
