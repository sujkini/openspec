# Baseline agents.md — refined each /eval-loop when routing gaps are found
#
# This file supplements (does not replace) target-repo AGENTS.md during eval analysis.
# Eval Generation updates this when constitution/plan/tasks agent routing gaps are identified.
#
# Round 0 — placeholder. Populate after first /eval-loop if gaps found.

## Agent routing notes

_PASTE_OR_AGENT_FILLED_AFTER_FIRST_LOOP_
